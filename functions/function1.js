function calculateCartprice(val1,al2,val3,...num1){
    return num1;
}

// console.log(calculateCartprice(2,2,3,4,5,6,7));

const user={
    username:"hitesh",
    price:199
}

function handleObject(anyobject)
{
    console.log(`username is ${anyobject.username} and price is ${anyobject.price}`);
}

// handleObject(user)
handleObject({
    username:"sam",
    price:199
})